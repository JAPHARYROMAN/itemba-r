// Moved to @/features/pos/core (POS remake phase 1). Kept so existing imports and tests
// stay byte-identical; remove when the classic and Kaunta shells retire (phase 6).
export * from '@/features/pos/core/pos-errors';
