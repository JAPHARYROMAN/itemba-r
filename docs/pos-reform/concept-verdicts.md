--- JUDGE usability ---
Kaunta — The Brass Counter: 8.5/10 — Fastest concept in the set for the person who matters: the app opens already selling, CASH is pre-selected, and the whole sale is 4 taps with the thumb parked on one 64px slab whose POSITION is the meaning — after a week the rep never reads the button. Picture→number→word hierarchy and haptic ticks mean a cart can be built essentially by feel. Deductions from the rep's own reality: (1) dark-navy default is wrong for a tin-roof duka in Dar sunlight — the light variant should be the default, not the escape hatch; (2) the 10px sync dot replaces today's amber text chip with a solid-vs-hollow-vs-grey distinction a cheap 720p LCD and a stressed rep will not reliably perceive — queue state gets LESS legible, not more; (3) the cart living behind an item-count chip tap (bottom sheet) is one step toward hidden — visible chip, so a small tax, but a new rep will hunt for 'where did my items go' on day one; (4) top module rail is less familiar than the bottom tabs every M-Pesa/WhatsApp user already knows. Manager story is solid (Manunuzi = sales grammar in reverse is genuinely low-training) but boot-into-Mauzo costs the receiving manager a rail tap every morning.
  keep: Boot directly into the sale builder — delete the home screen; this alone removes a tap and a screen-read from every single sale; CASH pre-selected on the payment screen (it is the modal case; one fewer tap on ~90% of sales); Single persistent bottom action whose screen position, not its label, carries the meaning — the strongest zero-reading device in any concept; Haptic vocabulary (tick on add, double-pulse on complete, buzz on reject) as the literacy-free feedback channel; Chenji as the single hero number the rep reads aloud — one number, nothing else, on the payment screen; Stoo filter chips Zote/Kidogo/Imeisha instead of a separate low-stock screen
SHABA — Kaunta ya Shaba: 8/10 — The best concept for COMPREHENSION and the only one that gets sunlight right: Mchana (near-black ink on warm paper) as the forced default is exactly what a rep outdoors on a cheap LCD needs, and the 17px body floor tested against Swahili strings (not English) shows the concept was designed for the actual reader. The MUHURI stamp is the strongest pre-literate 'it is done' signal proposed anywhere — a stamp already means official/finished to every Tanzanian, it fires on local commit so offline feels identical to online, and the amber IMEHIFADHIWA variant encodes sync truth without a word. The tally-row (groups of five) is the counting notation traders already use on cardboard — zero training. Bottom tab bar with a raised center Uza is the WhatsApp/M-Pesa pattern, so navigation needs no teaching. Why it loses to Kaunta on this lens: it never commits to booting into selling (Leo appears to be the landing tab — an extra tap and a screen to parse before every sale), never pre-selects CASH, and the one-product-per-screen blind-count wizard is honest but slow for a busy manager counting forty SKUs at closing time. The fixed duotone icon mnemonic set (notebook = Daftari = credit) is culturally exact and should be stolen regardless of winner.
  keep: Mchana light mode as the forced sunlight default with a persisted POS-local override — do not let system dark mode silently cost outdoor legibility; The MUHURI stamp: one multi-sensory completion ritual shared by all three modules, firing on LOCAL commit so feedback never waits on the network, with color (brass/amber) encoding sync truth; The tally row on the day screen — market-trader counting marks as the day's record; Duotone icon-chip mnemonic set, especially notebook = Daftari for CREDIT (the debt book is the culturally exact metaphor); Nothing under 14px, body raised to 17px, scale frozen against Swahili copy on a 320px screen; Turning the phone to show the customer the giant green Chenji — a trust gesture, not just a number
DUKA — Kaunta · Stoo · Daftari: 7/10 — The best MENTAL MODEL of the four — counter/storeroom/book is an IA a shopkeeper already carries in their head, so the navigation needs literally zero training, and Daftari la Leo is the single best answer to the scariest low-literacy question ('did my sale disappear?'): queued sales are just pencil-amber lines in the same book as sent ones, one screen, one truth. The always-visible status strip that deep-links to the queue from any room beats both slab-dot and home-chip approaches for legibility. Why it scores below the leaders on this lens: (1) real Next.js routes remount screens on every transition — on a Tecno that is perceptible jank on the most-repeated action of the day, and their own risk list admits tab bar + keyboard + cart bar can crush a 5-inch sale screen; (2) gating queue removal behind 'Mwambie msimamizi' with NO button strands a rep alone in a village shop with a stuck red row and nothing to press — for this user, a dead end is worse than an honor-system button; (3) Hesabu shows the system quantity grayed while counting — faster, but invites the lazy manager to type what the screen says, and precision-anchoring is exactly what the lens punishes; (4) no CASH pre-select, no boot-into-sale claim beyond Mauzo being the default tab. Solid, teachable, slightly slower everywhere.
  keep: Daftari la Leo: queue + my-sales + day summary collapsed into one ledger where queued sales are amber 'unposted' lines — the best offline-trust UI proposed by any concept; The duka rooms metaphor (Kaunta/Stoo/Daftari) as the naming and IA frame — zero-training information architecture; Global status strip visible from every screen that always jumps to the queue — sync state should never be one screen away; Success copy 'Imeandikwa kwenye daftari — itatumwa mtandao ukirudi' (written in the book) — custody language a shopkeeper trusts; Hide tabs when the sale stack or any text input is focused (the keyboard-crush mitigation) — needed by every tab-bar concept; Pre-warm Stoo/Leo fetches on tab-bar render so tab switches feel instant
Kaunta Imara: 6/10 — The deepest OFFLINE thinking and the shallowest SPEED thinking. Judged as the rep: the app boots to a hub of three doors — every sale starts with an extra tap and an extra screen to parse, which this lens punishes on the highest-frequency action. The coin metaphor is clever for engineers but risky for the actual user: the concept's own risk list admits a coin 'in the phone' can read as money physically stored on the device (theft fear, delete-the-app-lose-the-money fear), and coin/slip/sheet × brass/spinning/red is six token-state combinations to learn where SHABA teaches one stamp. Type-the-exact-amount to confirm a queue removal demands numeric precision from precisely the user we're told not to demand precision from. BUT — this concept alone noticed that counting happens in storerooms where signal dies, and its capture-offline/confirm-online count sheet is the only field-correct Hesabu in the set; its rejected-sale ritual (plain-Swahili error map, two big buttons: Jaribu tena / Mwite msimamizi, raw English collapsed for the supervisor) is the best error recovery proposed anywhere; and recoloring pending from warning-amber to calm brass ('offline is weather, not a warning') is the single most important emotional insight across all four documents. Cannibalize it heavily.
  keep: Reframe offline as custody, not warning: pending/queued states move OFF warning-amber onto calm brass — offline is Tuesday, and amber-everything trains anxiety; Capture-offline, confirm-online stock counting — counting happens in storerooms where signal dies; safe because systemQuantity is server-read at post time; The rejected-sale ritual: plain-Swahili error map in pos-i18n, exactly two big buttons (Jaribu tena / Mwite msimamizi), raw English server text collapsed behind a supervisor toggle; The parked purchase slip (kikaratasi): offline never dead-ends a manager into retyping twenty lines — save locally, explicit human confirm when network returns; The freshness clock with color aging (green→brass→grey) on stock snapshots, and greying out quantities entirely when a day stale; Designed offline photo fallback: brass initial-letter tile instead of a silently vanished image; Retry button on the Leo/my-sales fetch failure (today's leave-and-re-enter is a hidden gesture)

--- JUDGE feasibility ---
Kaunta — The Brass Counter: 8.5/10 — Tightest fit to the codebase with the smallest blast radius. Navigation is pushState/popstate inside the single existing mount at /mobile-pos, so the SW allowlist (verified: exact-path Set, navigate-mode only), proxy.ts middleware, the (dashboard) provider stack, and the isPosOnlyUser redirect are all untouched — the offline story is inherited, not re-earned. Theming is exactly the .pos-shell variable-override move Ground 3 certifies as trivial. Backend work is a verbatim lift of Ground 2's pre-scoped proposal (direct-Prisma /stock with forced terminal.branchId, [MPL-COUNT] notes-marker wrapper, the @Type/@Transform boolean gotcha cited by name). Every invariant is named and preserved, including the subtle ones (cart-survives-into-success, break-on-connection/skip-on-rejection sync, frequents counting queued sales, honor-system queue removal kept honor-system). Migration is genuinely incremental: hooks-first extraction with UI untouched, ship, then re-skin per screen; the Slab is one new component using only compiled Tailwind animations. Perf posture is honest — no new deps, transform/opacity only, glows as box-shadows. Deductions: dark-by-default is the one risky product call for sunlit shops (hedged with light-variant-day-one, but it is still a default flip), boot-into-sale changes manager landing behavior, and the 4-tap claim depends on CASH pre-selection that must not mis-fire for credit-heavy terminals. Risk register is the most codebase-literate of the four (SW update-lag rollout compat, popstate vs QR-login-redirect interplay).
  keep: pushState/popstate history inside the single mount — Android back support with zero SW/middleware/layout changes; Hooks-first refactor sequencing: extract usePosBootstrap/usePosOutbox/usePosCart with UI untouched, ship, then re-skin; Sync dot as ambient queue state (solid green / pulsing brass / grey) replacing the amber text chip; Routing top rejection-error shapes through pos-i18n so queue cards stop leaking raw English at reps; Positional disambiguation rule for brass vs warning-amber (warnings only in the top ribbon, brass only in numerals/celebration); Hiding exact stock quantities when the offline snapshot is >24h stale, showing status dot only
SHABA — Kaunta ya Shaba: 8/10 — Engineering-equivalent to Kaunta on the load-bearing decisions — pushState routing with no new URLs (explicitly so POS_ROUTES and middleware stay untouched), .pos-shaba scoped variable re-theme, verbatim Ground 2 backend, PosLangProvider, QuantityInput kept for its proven clamp logic — and it adds two things Kaunta lacks: the most explicit incremental sequencing plan of the four (theme/manifest/fonts first at near-zero risk, refactor second, features last) and a named requirement for characterization tests on each load-bearing quirk before the split. Mchana-light-by-default is the more honest sunlight call than Kaunta's dark default. Blind counts (never show systemQuantity) align perfectly with the server-authoritative design. Deductions for scope creep beyond the essentials: WebAudio earcons (off by default, but still new surface), a one-product-per-screen count wizard (more screens to build and translate), a commissioned maskable stamp icon (correctly de-risked as non-blocking), one new Tailwind keyframe, a new local day-tally IDB counter, and the total removal of blue CTAs — engineering-trivial but a retraining cost on muscle memory the concept itself elsewhere claims to protect. Risk register is strong and specific (back-map per screen to prevent Malipo re-pop, liveStock cost-field strip as review-blocking, sw-copy-first string sizing).
  keep: Mchana light-mode-by-default for direct sunlight, with the mode override shipped in the same PR as the theme; Characterization tests for each load-bearing quirk (cart-survival, sync-skip-vs-break, frequents-counts-queued, offline cold start) before touching the monolith; Blind stock counts — never show system quantity before entry; prevents anchoring and matches server-authoritative systemQuantity; Explicit per-screen back-map, with success-screen back going to home, never re-popping payment; Brass initial-letter tile as the designed offline fallback for product photos instead of silent onError collapse; The five-phase rollout sequencing (visible identity first, refactor second, signature element third, backend feature fourth); Swahili-written-first i18n keys, tested at the type-scale floor on 320px screens
Kaunta Imara: 6.5/10 — The core skin/backend/refactor plan is sound and grounded (pushState option, .pos-skin overrides, Ground 2 endpoints verbatim, module-level sale store to preserve cart-into-success, self-hosted font), and the coin/tray metaphor is implementable with the compiled animation vocabulary. But it carries the largest new-risk surface of the four. The parked-drafts system (offline purchase slips + offline count sheets in a new drafts store) is exactly the offline write surface for money-bearing documents that the other three concepts — and the current backend design — deliberately avoid; human-confirm-on-send mitigates but the concept itself admits 48h-stale drafts, capture-time-vs-post-time variance surprises, and expiry states, which is a lot of new state machine to test on top of the riskiest refactor. The error-code map is built on a backend that emits free-text English with no error codes (acknowledged as best-effort — meaning real backend work or a fragile pattern-match layer, neither scoped). Routing is hedged ('history API ... or Next parallel segments') — the second option inherits DUKA's SW problem and the ambiguity itself is a planning gap. The semantic recolor of pending states from warning-amber to brass is a defensible reframe but touches the one color-confusion axis every concept flags, while removing the existing learned amber=pending signal. Type-the-amount removal friction and per-product detail screens add further scope. Excellent honesty in the risk register (metaphor misread pilot, snapshot trust, first-load-online), which keeps the score from falling further.
  keep: Offline-as-custody reframe: stop styling routine offline/pending states as warnings; reserve red for server-rejected sales that need a human; Rejected-sale ritual: per-item retry button, plain-Swahili explanation, raw English server text collapsed behind a technical-details toggle; Designed offline photo fallback (initial-letter disc) formalizing today's onError collapse; Freshness clock with aging colors (minutes=green, hours=brass, day+=grey with quantities hidden) on the stock snapshot; Retry button on My-Sales-Today fetch failure, fixing the leave-and-re-enter-only retry; Problems-first default sort (OUT, LOW, OK) on the stock list; Distinct haptic signatures for sent-vs-queued sale completion as literacy-free sync feedback; 'Held in tray: N' line on the day screen so sent+held reconciles against the cash drawer
DUKA — Kaunta · Stoo · Daftari: 5.5/10 — The information architecture is arguably the best of the four for the audience (one duka metaphor, the Daftari merging queue+mySales+day-summary into one reassuring ledger), but the load-bearing engineering decision — real Next.js routes under /mobile-pos/* — is the wrong one for this codebase and gets punished under this lens. Verified against the actual SW: the allowlist is an exact-path Set matched only on navigate-mode requests, so the concept's own fix (prefix-match + cache bump, shipped in lockstep) covers only full-document loads; App Router client-side navigations fetch RSC flight payloads as non-navigate GETs the SW ignores by design, meaning offline tab-switching breaks in a way the concept never analyzes — its acknowledged 'SW/route skew bricks offline' risk understates the real failure mode. Real routes also force a coordinated atomic release (SW + routes + cache name) instead of incremental migration, and add per-navigation remount cost on low-end Android that the concept itself flags and then mitigates with provider-hoisting complexity the pushState concepts get for free. Second unforced error: converting queue removal from honor-system to enforced manager gating is a behavior change requiring a session flag/permission that its backend section never scopes, and it risks stranding a rep with a bad queued sale (acknowledged, half-mitigated). Everything else — .pos-duka theming, Ground 2 backend verbatim, seam-based decomposition, snapshot throttling — is solid, which is why this isn't lower.
  keep: The Daftari: queued offline sales rendered as amber-edged rows inside the same today-ledger as sent sales — queue, my-sales, and day summary as one truth, defusing 'did my sale disappear?'; Global status strip visible from every screen, deep-linking to the queue section; Ruled-ledger background as a pure CSS gradient — zero assets, zero SW impact; Throttling stock-snapshot refresh to on-tab-open plus max-age instead of every catalog sync (IDB write pressure on cheap storage); Hiding the tab bar whenever a text input is focused or a flow stack is open (keyboard + tab bar + cart bar crush on 5-inch screens); Pre-warming Stoo/Leo fetches on tab-bar render so tab switches feel instant; Pairing any future enforced queue-removal gating with a failed-row details view so support can diagnose remotely; The rule that Stoo keeps stripping all money fields because rep phones get stolen

--- JUDGE distinction ---
Kaunta Imara: 9/10 — The only concept whose identity IS the product story. 'Offline is weather, not a warning' is a genuine design thesis, and everything downstream is derived from it rather than decorated onto it: the four-color semantic system (brass=in your custody, blue=press me, green=arrived, red=needs a person) is the cleanest and least ambiguous of the four; the coin/tray/Ukanda isn't a brand flourish, it is literally the UI for the three hardest-to-explain mechanisms (idempotent queueing, sync progress, rejection recovery), taught wordlessly by a 400ms transform. The 'sync-legibility law' — every unsent datum is one counted, colored token in one fixed place with exactly three states — is a real system law, the kind of constraint state-of-the-art design is made of. Distinct haptic signatures for sent-vs-held is literacy-free feedback no competitor does. It also goes furthest functionally (kikaratasi parked drafts turn offline purchases/counts from dead-ends into rituals) while being honest about the risks it creates. Deductions: the surface treatment (navy + brass, chunky radii) is interchangeable with the other three, so the static visual identity leans heavily on one metaphor; and the coin-in-phone misread (money stored on the device → theft fear) is a real cultural hazard the concept itself flags but can only pilot-test away. Least wallpaper-like of the four: remove the coin system and the app stops working, which is the test decoration always fails.
  keep: The core reframe: offline/pending recolored from warning-amber to custody-brass; red reserved solely for 'needs a human' — adopt regardless of winning concept; The sync-legibility law: every unsent local datum is one colored token (coin/slip/sheet) in one fixed place with exactly three states; Ukanda status belt: network dot + tray count + freshness clock ambient on every screen, tappable to the queue; Coin-mint animation that flies up (sent) or drops into the tray (held) — the sync model taught without words; Kikaratasi parked drafts for offline purchases/counts with explicit human confirm at send, freshness clock, and visible 48h expiry; posErrorMessage() code→Swahili map with raw English collapsed behind 'Maelezo ya kiufundi' for the supervisor; Type-the-sale-amount friction as a literacy-free confirm on queue removal, plus full line inspection of queued sales; Freshness clock with aging colors (green→brass→grey) on the stock snapshot; grey-out quantities at 'jana au zaidi'; Distinct haptic signatures for 'sent' vs 'held in tray' — state through the palm; Brass coin doubles as the maskable app icon and theme color — identity and offline model as one object
Kaunta — The Brass Counter: 8.5/10 — The strongest structural signature and the best speed story. The Kaunta Slab — one fixed object fusing total, primary verb, sync state, and celebration — is a genuinely novel POS element: position becomes meaning, the thumb never relocates, and after a week the rep completes sales without reading. Boot-into-sale + CASH pre-selected collapsing the modal sale to 4 taps is the single most valuable UX decision in any of the four concepts, and the 'picture → number → word' hierarchy is the right low-literacy law stated crisply. Discipline is excellent: every motion is feedback, the two glow moments are budgeted, brass/amber confusion is solved structurally (position + shape, not hope). Two deductions keep it off the top. First, dark-by-default is a legibility gamble in exactly the wrong context — Dar es Salaam sunlight on cheap low-nit LCDs — and SHABA's opposite argument is more convincing; a concept this disciplined making its one aesthetic-first call at the expense of the primary environment is a coherence crack. Second, its identity is the least differentiated from siblings: same navy, same brass-for-money, blue verbs retained — a screenshot reads as 'the ERP, but dark.' The top module rail is defensible (frequency-based zoning) but weaker than a reachable bottom bar for the Stoo/Leo switch. Still: nothing here is wallpaper; every element earns its place.
  keep: The Kaunta Slab: one persistent bottom object = total + single primary verb + sync dot + celebration flash; position as meaning; Boot directly into the sale builder + CASH pre-selected: the 4-tap modal sale — adopt in any winning concept; Slab sync dot (solid green / pulsing hollow brass / grey) replacing the text queue chip; tap → queue; Structural disambiguation of brass vs warning-amber: warnings only in the top ribbon with icon, brass only in numerals/celebration; Counter tray: cart as a bottom sheet over the grid instead of a competing list, reusing QuantityInput verbatim; The 'picture → number → word' low-literacy hierarchy with names visually subordinate to photos and prices; Count-up tick on the slab total as add-to-cart feedback you don't have to look up for; Hide exact stock quantities >24h stale, show status dot only; 'Mwisho kuonekana' (last seen) copy rule — never present-tense certainty on snapshot data; Documented one-flag escape hatch from auto-posted stock counts to stop-at-PENDING_APPROVAL
SHABA — Kaunta ya Shaba: 8/10 — The most memorable brand artifact and the sharpest cultural eye. The MUHURI stamp is the single best image in the whole exercise — pre-literate, multi-sensory, offline-honest (amber stamp for 'saved on this phone' carries sync truth without anxiety), unifying all three modules under one ritual — and the tally-row accrual in market-trader five-stroke notation is a stroke of genuine cultural specificity, as are the daftari notebook icon for CREDIT and blind stock counting. It also makes the correct legibility call the others fudge: Mchana light-default for direct sunlight, with the dark mode as the option. Deleting blue entirely is the boldest distinctiveness move — a SHABA screenshot is unmistakable. But that boldness opens the concept's two coherence cracks: it discards the one piece of trained muscle memory (blue = press me) the fleet already has, and in Usiku mode brass becomes the primary CTA color — so brass now means 'money', 'celebration', 'the Uza tab', AND 'press me', directly violating the concept's own §2.1 discipline rule. A design language whose signature color means four things in dark mode is less state-of-the-art than one whose four colors mean one thing each. The stamp is also a terminal moment rather than an ambient system — the tally row partially rescues this, but between sales the identity carries less structural weight than the Slab or the coin tray. Secondary: WebAudio earcons are admitted garnish, correctly shipped off-by-default.
  keep: MUHURI stamp: one completion ritual across all modules, with color encoding sync truth (brass IMELIPWA vs amber IMEHIFADHIWA); Tally row on Leo in four-strokes-and-a-diagonal market notation — the day's work made physical; Mchana (light) as sunlight default regardless of system theme, persisted like the language toggle — the correct call for this fleet; Blind stock counting: never show system quantity before entry (prevents anchoring, matches server-authoritative posture); Daftari notebook icon for CREDIT — culturally exact metaphor for the shopkeeper's debt book; Fixed duotone icon-chip mnemonic set so payment methods and modules are learnable once, never text-only; Test the type scale against Swahili strings at 320px before freezing — sw runs long; 17px body floor and nothing under 14px anywhere on cheap screens; Brass initial-letter tile as the designed offline photo fallback instead of a silently vanished image; Both stamp feedback channels fire on local commit, never waiting on the network; Swahili written first, English translated from it, for all new keys
DUKA — Kaunta · Stoo · Daftari: 7/10 — The best information architecture and the weakest visual identity. The three-rooms metaphor (counter / storeroom / ledger) is the cleanest mental model offered — nothing abstract, nothing 'ERP' — and Daftari la Leo is a superb piece of unification: queue + my-sales + day summary collapsed into one culturally-loaded object, converting the scariest offline question ('did my sale disappear?') into the most reassuring answer ('it's written in the book'). The pencil-amber unposted rows and the double brass underline at close are the concept's one moment of real design distinction. But outside that single tab, DUKA is a competent conservative re-theme: blue CTAs kept, brass rationed to 'money moments', standard bottom tabs, standard success glow — the sale flow itself, where the rep lives all day, has no signature element and no ritual, which is exactly the 'generic mobile-app wallpaper' failure mode the lens punishes. It also takes the most implementation risk (real Next.js routes requiring lockstep SW allowlist changes, with a self-acknowledged brick-the-offline-app failure mode) for the least identity payoff — history-backed pseudo-routing buys the same back-button win at a fraction of the risk, as both Kaunta concepts show. Practical, shippable, humane; not state-of-the-art.
  keep: Daftari la Leo: the day as a ruled exercise book where queued sales are pencil-amber 'unposted' lines in the same list as sent ones — one metaphor doing three jobs; strongly consider porting this into the winning concept's Leo screen; Ruled-line ledger background as pure CSS gradient — a visual fingerprint for zero assets; Global status strip visible from every room, deep-linking to the queue section; 'Imeandikwa kwenye daftari — itatumwa mtandao ukirudi' custody copy for queued-sale success; Hide the tab bar whenever the sale flow stack or a text input is focused (keyboard + tabs + cart bar crush 5-inch screens); Queue removal gated by session flag with 'Mwambie msimamizi' for reps — paired with a failed-row details view for remote diagnosis; Throttle the stock snapshot to on-tab-open + max-age rather than every catalog sync (low-end storage discipline); Pre-warm Stoo/Leo fetches on tab-bar render so tab switches feel instant on low-end Android; Hold-the-line rule stated as policy: rep phones get stolen, so the stock endpoint never ships cost/value fields